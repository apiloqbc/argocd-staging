apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "level-base.fullname" . }}-test-connection"
  labels:
    {{- include "level-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "level-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
