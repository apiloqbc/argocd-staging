apiVersion: v1
kind: Service
metadata:
  name: {{ include "level-base.fullname" . }}
  labels:
    {{- include "level-base.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "level-base.selectorLabels" . | nindent 4 }}
